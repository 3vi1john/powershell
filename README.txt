-First set excecution policy in powershell to unrestricted
Start, Run, Search for powershell, right click run as administrator

When the powershell prompt opens type Set-ExcecutionPolicy unrestricted,type y, then hit enter.
You can now run the script.